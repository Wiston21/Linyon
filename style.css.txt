body {
  font-family: sans-serif;
  background: linear-gradient(to bottom, #00209f, #d21034);
  color: #fff;
  margin: 0;
  padding: 0;
}

header {
  text-align: center;
  padding: 1.5rem;
}

main {
  max-width: 500px;
  margin: 0 auto;
  background: rgba(0, 0, 0, 0.4);
  padding: 1.5rem;
  border-radius: 12px;
}

section {
  margin-bottom: 1.5rem;
}

input, textarea, button {
  width: 100%;
  padding: 0.75rem;
  margin: 0.4rem 0;
  border-radius: 8px;
  border: none;
}

button {
  background: #ffd700;
  color: #000;
  font-weight: bold;
  cursor: pointer;
}

.hidden {
  display: none;
}

.match-card {
  background: rgba(255, 255, 255, 0.1);
  padding: 0.75rem;
  border-radius: 8px;
  margin-bottom: 0.5rem;
}

#chatMessages {
  background: rgba(0, 0, 0, 0.3);
  min-height: 150px;
  max-height: 250px;
  overflow-y: auto;
  padding: 0.5rem;
  border-radius: 8px;
  margin-bottom: 0.5rem;
}

.chat-message {
  margin-bottom: 0.4rem;
}

.chat-message.me {
  text-align: right;
  color: #ffd700;
}

.chat-message.them {
  text-align: left;
  color: #fff;
}

#chatHeader {
  margin-bottom: 0.5rem;
  font-weight: bold;
}
